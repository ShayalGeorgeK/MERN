<ul>
    <% products.forEach((product) => { %>
        <li>
            <%= product %>
        </li>
    <% }) %>
</ul>


<% if (products.length > 0) { %>
    <p>products available</p>    
<% }else{ %>
    <p>No products</p>
<% } %>